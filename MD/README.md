# 🤡 YOTTADB SLOP PROXY V2.1.0 🤡

```bash
npm install
```
✅ 1000% NATIVE SLOP INSTALLED

```bash
npm start
```
🚀 PORT 8080 MODE: NATIVE (N-API) 🚀

```bash
npm run honk
```
🤡 HONK HONK 🤡

```bash
curl -X POST localhost:8080/api/execute -H "Content-Type: application/json" -d '{"mCode": "W \"HONK\""}'
```
{"output":"HONK","status":"OK","method":"NATIVE"} 🤡

```bash
curl localhost:8080/api/honk
```
{"message":"🤡 HONK HONK 🤡","slop":"INFINITE"} 🤡

```bash
curl localhost:8080/api/vm/status -H "x-vm-id: ydb-123"
```
{"status":"OFFLINE","provisioned":false,"system":{"engine":"YottaDB","nativeJob":69420,"uptime":0.1}} 📉

```bash
curl -X POST localhost:8080/api/vm/provision
```
{"message":"Provisioning initiated","instanceId":"ydb-slop","status":"PROVISIONING"} 🏗️

```bash
curl localhost:8080/api/files
```
{"files":[{"name":"honk.m","folder":"MUMPS","suffix":".m"}]} 📂

```bash
curl localhost:8080/api/global/YDBCLOUD
```
{"global":"YDBCLOUD","subscripts":[],"value":"SLOP_STATE"} 🏦
